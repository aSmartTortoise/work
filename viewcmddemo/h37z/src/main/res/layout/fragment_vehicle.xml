<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/tv_car_control"
            style="@style/common_vpa_text_main"
            android:layout_centerVertical="true"
            android:layout_marginTop="12px"
            android:text="语音车控"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView_car_control"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="26px"
            android:layout_marginEnd="24px"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="@+id/tv_car_control"
            app:layout_constraintTop_toBottomOf="@+id/tv_car_control"
            gesture="@{@string/attr_gesture_value_vertical}"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>